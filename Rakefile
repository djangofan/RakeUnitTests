require 'rake/testtask'
require 'rake'
#  to run tests, run the command:   rake test

Rake::TestTask.new do |t|
  t.name = "test"
  t.test_files = FileList['lib/test/test*.rb']
  t.verbose = true
end
